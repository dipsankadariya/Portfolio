const navElements = document.querySelectorAll('.nav-elements');
  
navElements.forEach(navElement => {
  navElement.addEventListener('click', event => {
    event.preventDefault();
alert(`Under construction.
Will be updated shortly.
Thank you`);
  });
});


const clonedProjects = document.querySelectorAll('.cloned-projects');

clonedProjects.forEach(clonedProjects => {
  clonedProjects.addEventListener('click',event=>
  {
event.preventDefault();
alert(`Under construction.
Will be updated shortly.
Thank you`);
  });
});

const webProjects = document.querySelectorAll('.web-projects');

webProjects.forEach(webProjects =>{
  webProjects.addEventListener('click',event =>
  {
    event.preventDefault();
alert(`Under construction.
Will be updated shortly.
Thank you`);
  });
});


const minProject=document.querySelectorAll('.min-project');

minProject.forEach(minProject =>{
  minProject.addEventListener('click',event=>
  {
alert(`Under construction.
Will be updated shortly.
Thank you`);  
  });
});

